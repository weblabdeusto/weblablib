language: python
python:
  - 2.7
  - 3.3
  - 3.4
  - 3.5
  - 3.6
install:
  - python setup.py install
  - pip install coveralls nose
script:
  coverage run --source=weblablib.py -m nose
after_success:
  coveralls
